
### encrypt-file "$target"
```bash
  encrypt-file "$target
  local _base="$(get-base-file-name $target)"
  target="${_base}.enc"
```
**file1.txt --> file1.enc**

### gpg-encrypt-file "$target"
```bash

```

gpg-encrypt-file "$target"
